services:
  - type: web
    name: openscienceenigma
    env: node
    region: oregon
    buildCommand: npm --prefix server install && npm run build:admin
    startCommand: npm --prefix server start
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
